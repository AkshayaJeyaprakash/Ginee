class ginee:

    def kill(ad_id, ctr, budget):
        max_ctr = max(ctr)
        index_max = ctr.index(max_ctr)
        retain = ad_id[index_max]
        return retain

    def json_list(json):
        return ad_id, ctr, budget

    def dict_list(dict1):
        return ad_id, ctr, budget

    def excel_list(filename):
        return ad_id, ctr, budget
